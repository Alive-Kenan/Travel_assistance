# 文本输入分析实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让首页文本输入支持“景点关键词”和“攻略段落”两种纯文本分析，并对抖音链接 / 口令做明确拦截。

**Architecture:** 新增一个正式的文本分析服务作为 `parseNow()` 的唯一入口，先对输入做分类，再分别走关键词路由或长文本提炼路由。两条文本路由都尽量复用现有 `Stage1SpotExtraction -> enrichSpotWithKimi -> 稀疏栏目二次补全` 能力，同时在 Kimi 失败时保留本地基础结果。

**Tech Stack:** React 18、TypeScript、Zustand、Vitest、现有 Kimi Moonshot API 客户端

---

### Task 1: 输入分类模块

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\textInputClassifier.ts`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\textInputClassifier.test.ts`

- [ ] **Step 1: 写失败测试，覆盖三类输入**

```ts
import { describe, expect, it } from "vitest"
import { classifyTextInput } from "@/utils/textInputClassifier"

describe("classifyTextInput", () => {
  it("classifies douyin url as url_or_token", () => {
    expect(
      classifyTextInput("https://v.douyin.com/abc123/"),
    ).toMatchObject({ kind: "url_or_token" })
  })

  it("classifies short scenic query as keyword_query", () => {
    expect(
      classifyTextInput("雁荡山 门票 住宿 美食"),
    ).toMatchObject({ kind: "keyword_query" })
  })

  it("classifies long travel note as travel_note_text", () => {
    expect(
      classifyTextInput("雁荡山适合安排一日游，上午先去灵岩景区，中午在响岭头吃麦饼，下午再去大龙湫。"),
    ).toMatchObject({ kind: "travel_note_text" })
  })
})
```

- [ ] **Step 2: 跑测试，确认先红**

Run: `npm test -- src/utils/textInputClassifier.test.ts`
Expected: FAIL，提示 `Cannot find module '@/utils/textInputClassifier'`

- [ ] **Step 3: 写最小实现**

```ts
export type TextInputKind = "url_or_token" | "keyword_query" | "travel_note_text"

export function classifyTextInput(text: string): { kind: TextInputKind } {
  const normalized = text.trim()

  if (
    /https?:\/\/\S+/i.test(normalized) ||
    /douyin|抖音|复制此链接|打开抖音|口令/i.test(normalized)
  ) {
    return { kind: "url_or_token" }
  }

  if (normalized.length <= 24 && !/[。；;！!？?\n]/.test(normalized)) {
    return { kind: "keyword_query" }
  }

  return { kind: "travel_note_text" }
}
```

- [ ] **Step 4: 跑测试，确认转绿**

Run: `npm test -- src/utils/textInputClassifier.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/utils/textInputClassifier.ts src/utils/textInputClassifier.test.ts
git commit -m "feat: add text input classifier"
```

### Task 2: 关键词查询提炼

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\textKeywordQuery.ts`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\textKeywordQuery.test.ts`

- [ ] **Step 1: 写失败测试，覆盖景点名和意图词提取**

```ts
import { describe, expect, it } from "vitest"
import { extractKeywordQueryStage1 } from "@/utils/textKeywordQuery"

describe("extractKeywordQueryStage1", () => {
  it("builds a lightweight stage1 payload from short keywords", () => {
    expect(
      extractKeywordQueryStage1("雁荡山 门票 住宿 美食"),
    ).toMatchObject({
      coreSpotName: "雁荡山",
      transportHints: [],
      stayHints: ["用户关注住宿信息"],
      tips: ["用户关注门票信息", "用户关注美食信息"],
      confidence: "medium",
    })
  })
})
```

- [ ] **Step 2: 跑测试，确认先红**

Run: `npm test -- src/utils/textKeywordQuery.test.ts`
Expected: FAIL，提示模块不存在

- [ ] **Step 3: 写最小实现**

```ts
import type { Stage1SpotExtraction } from "@/types/spotGuide"

const CITY_WORDS = ["杭州", "上海", "北京", "广州", "深圳", "成都", "重庆", "西安", "南京", "苏州", "厦门", "青岛", "昆明", "大理", "温州"]

export function extractKeywordQueryStage1(text: string): Stage1SpotExtraction {
  const tokens = text.split(/\s+/).map((item) => item.trim()).filter(Boolean)
  const coreSpotName = tokens[0] ?? text.trim()
  const city = CITY_WORDS.find((item) => text.includes(item))
  const stayHints = /住宿|酒店|民宿|住哪/i.test(text) ? ["用户关注住宿信息"] : []
  const transportHints = /交通|高铁|自驾|地铁|公交/i.test(text) ? ["用户关注交通信息"] : []
  const tips = [
    /门票|购票|预约/i.test(text) ? "用户关注门票信息" : "",
    /美食|小吃|文创/i.test(text) ? "用户关注美食信息" : "",
    /路线|动线|一日游/i.test(text) ? "用户关注路线信息" : "",
  ].filter(Boolean)

  return {
    coreSpotName,
    city,
    summary: "",
    highlights: [],
    checkpoints: [],
    foods: [],
    souvenirs: [],
    nearbyCandidates: [],
    transportHints,
    stayHints,
    tips,
    confidence: "medium",
  }
}
```

- [ ] **Step 4: 跑测试，确认转绿**

Run: `npm test -- src/utils/textKeywordQuery.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/utils/textKeywordQuery.ts src/utils/textKeywordQuery.test.ts
git commit -m "feat: add keyword query extraction"
```

### Task 3: 长文本提炼为 stage1

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\textStage1Extractor.ts`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\textStage1Extractor.test.ts`

- [ ] **Step 1: 写失败测试，覆盖长文本提炼**

```ts
import { describe, expect, it } from "vitest"
import { extractTravelNoteStage1 } from "@/utils/textStage1Extractor"

describe("extractTravelNoteStage1", () => {
  it("extracts scenic clues from a travel note paragraph", () => {
    const result = extractTravelNoteStage1(
      "雁荡山适合安排一日游，上午先去灵岩景区，中午在响岭头吃麦饼，下午去大龙湫，建议提前预约门票。",
    )

    expect(result).toMatchObject({
      coreSpotName: "雁荡山",
      checkpoints: ["灵岩景区", "大龙湫"],
      foods: ["麦饼"],
      tips: expect.arrayContaining(["建议提前预约门票。"]),
      confidence: "medium",
    })
  })
})
```

- [ ] **Step 2: 跑测试，确认先红**

Run: `npm test -- src/utils/textStage1Extractor.test.ts`
Expected: FAIL，提示模块不存在

- [ ] **Step 3: 写最小实现**

```ts
import type { Stage1SpotExtraction } from "@/types/spotGuide"

function pickFirstMatch(text: string, items: string[]) {
  return items.find((item) => text.includes(item)) ?? ""
}

export function extractTravelNoteStage1(text: string): Stage1SpotExtraction {
  const scenicNames = ["雁荡山", "夫子庙秦淮风光带", "西湖", "黄山"]
  const checkpointNames = ["灵岩景区", "大龙湫", "灵峰", "方洞", "文德桥", "老门东"]
  const foodNames = ["麦饼", "瘦肉丸", "鸭血粉丝汤", "盐水鸭"]
  const coreSpotName = pickFirstMatch(text, scenicNames) || text.slice(0, 12)
  const checkpoints = checkpointNames.filter((item) => text.includes(item))
  const foods = foodNames.filter((item) => text.includes(item))
  const tips = text
    .split(/[。！？!?]/)
    .map((item) => item.trim())
    .filter((item) => /(门票|预约|注意|排队|开放|购票)/.test(item))
    .map((item) => `${item}${item.endsWith("。") ? "" : "。"}`)

  return {
    coreSpotName,
    city: text.includes("温州") ? "温州" : undefined,
    summary: text.slice(0, 80),
    highlights: [],
    checkpoints,
    foods,
    souvenirs: [],
    nearbyCandidates: [],
    transportHints: [],
    stayHints: [],
    tips,
    confidence: "medium",
  }
}
```

- [ ] **Step 4: 跑测试，确认转绿**

Run: `npm test -- src/utils/textStage1Extractor.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/utils/textStage1Extractor.ts src/utils/textStage1Extractor.test.ts
git commit -m "feat: add travel note stage1 extraction"
```

### Task 4: 正式文本分析服务

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\textGuideAnalyzer.ts`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\textGuideAnalyzer.test.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\kimiClient.ts`

- [ ] **Step 1: 写失败测试，覆盖三条文本路由**

```ts
import { describe, expect, it, vi } from "vitest"
import { analyzeTextGuide } from "@/services/textGuideAnalyzer"

vi.mock("@/services/kimiClient", () => ({
  enrichSpotWithKimi: vi.fn(),
}))

describe("analyzeTextGuide", () => {
  it("rejects douyin input early", async () => {
    await expect(
      analyzeTextGuide("https://v.douyin.com/abc123/"),
    ).rejects.toThrow("当前版本仅支持纯文本分析")
  })

  it("supports short keyword input", async () => {
    const result = await analyzeTextGuide("雁荡山 门票 住宿")
    expect(result.guide.coreSpot.title).toBeTruthy()
  })

  it("supports long travel note input", async () => {
    const result = await analyzeTextGuide("雁荡山适合安排一日游，上午灵岩景区，下午大龙湫。")
    expect(result.guide.coreSpot.title).toBeTruthy()
  })
})
```

- [ ] **Step 2: 跑测试，确认先红**

Run: `npm test -- src/services/textGuideAnalyzer.test.ts`
Expected: FAIL，提示模块不存在

- [ ] **Step 3: 写最小实现**

```ts
import { enrichSpotWithKimi } from "@/services/kimiClient"
import type { SpotGuide } from "@/types/spotGuide"
import { extractKeywordQueryStage1 } from "@/utils/textKeywordQuery"
import { classifyTextInput } from "@/utils/textInputClassifier"
import { extractTravelNoteStage1 } from "@/utils/textStage1Extractor"
import { mapStage1ToSpotGuide } from "@/utils/spotGuideMapper"

export async function analyzeTextGuide(inputText: string): Promise<{
  guide: SpotGuide
  partialMessage: string | null
}> {
  const normalized = inputText.trim()
  if (!normalized) {
    throw new Error("请输入抖音链接、口令或攻略文本")
  }

  const classification = classifyTextInput(normalized)
  if (classification.kind === "url_or_token") {
    throw new Error("当前版本仅支持纯文本分析，请粘贴景点名、关键词或一段攻略文字")
  }

  const stage1 =
    classification.kind === "keyword_query"
      ? extractKeywordQueryStage1(normalized)
      : extractTravelNoteStage1(normalized)

  try {
    const guide = await enrichSpotWithKimi(stage1)
    return { guide, partialMessage: null }
  } catch {
    return {
      guide: mapStage1ToSpotGuide(stage1),
      partialMessage: "联网补充失败，当前展示基础结果",
    }
  }
}
```

- [ ] **Step 4: 跑测试，确认转绿**

Run: `npm test -- src/services/textGuideAnalyzer.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/services/textGuideAnalyzer.ts src/services/textGuideAnalyzer.test.ts
git commit -m "feat: add text guide analyzer service"
```

### Task 5: 接入 store 文本入口

**Files:**
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.test.ts`

- [ ] **Step 1: 写失败测试，覆盖 parseNow 改走正式文本服务**

```ts
it("uses text guide analyzer for text input instead of mock parser", async () => {
  analyzeTextGuideMock.mockResolvedValue({
    guide: spotGuideFixture,
    partialMessage: null,
  })

  useRideGuideStore.getState().setInputText("雁荡山 门票 住宿")
  await useRideGuideStore.getState().parseNow()

  expect(analyzeTextGuideMock).toHaveBeenCalledWith("雁荡山 门票 住宿")
  expect(useRideGuideStore.getState().status).toBe("success")
})
```

- [ ] **Step 2: 跑测试，确认先红**

Run: `npm test -- src/store/useRideGuideStore.test.ts`
Expected: FAIL，提示没有调用 `analyzeTextGuide`

- [ ] **Step 3: 写最小实现**

```ts
import { analyzeTextGuide } from "@/services/textGuideAnalyzer"

parseNow: async () => {
  controller?.abort()
  controller = new AbortController()

  set({ status: "loading", error: null, partialMessage: null })
  const { inputText } = get()

  try {
    const result = await analyzeTextGuide(inputText)

    if (controller.signal.aborted) return

    set({
      status: "success",
      result: result.guide,
      partialMessage: result.partialMessage,
    })
  } catch (error) {
    if (controller.signal.aborted) return

    set({
      status: "error",
      error: {
        code: "INVALID_INPUT",
        message: error instanceof Error ? error.message : "文本解析失败",
      },
    })
  }
}
```

- [ ] **Step 4: 跑测试，确认转绿**

Run: `npm test -- src/store/useRideGuideStore.test.ts`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/store/useRideGuideStore.ts src/store/useRideGuideStore.test.ts
git commit -m "feat: connect text analysis flow to store"
```

### Task 6: 输入区文案与拦截体验

**Files:**
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\components\InputPanel.tsx`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\pages\Home.copy.test.tsx`

- [ ] **Step 1: 写失败测试，覆盖输入区提示文案**

```ts
it("shows text-only hint for the first release", () => {
  const markup = renderToStaticMarkup(
    <InputPanel
      value=""
      onChange={() => {}}
      onParse={() => {}}
      onClear={() => {}}
      onExample={() => {}}
    />,
  )

  expect(markup).toContain("当前版本支持景点名、关键词和攻略文字；抖音链接 / 口令暂未开放。")
})
```

- [ ] **Step 2: 跑测试，确认先红**

Run: `npm test -- src/pages/Home.copy.test.tsx`
Expected: FAIL，提示文案不匹配

- [ ] **Step 3: 写最小实现**

```tsx
<div className="mt-1 text-xs text-ink-500">
  当前版本支持景点名、关键词和攻略文字；抖音链接 / 口令暂未开放。
</div>
```

- [ ] **Step 4: 跑测试，确认转绿**

Run: `npm test -- src/pages/Home.copy.test.tsx`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/components/InputPanel.tsx src/pages/Home.copy.test.tsx
git commit -m "chore: clarify text-only input support"
```

### Task 7: 全链路回归

**Files:**
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\textGuideAnalyzer.test.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.test.ts`

- [ ] **Step 1: 补完整回归测试**

```ts
it("falls back to a mapped stage1 guide when kimi enrichment fails", async () => {
  enrichSpotWithKimiMock.mockRejectedValue(new Error("SEARCH_FAILED"))

  const result = await analyzeTextGuide("雁荡山适合安排一日游，上午灵岩景区，下午大龙湫。")

  expect(result.partialMessage).toBe("联网补充失败，当前展示基础结果")
  expect(result.guide.coreSpot.title).toContain("雁荡山")
})
```

- [ ] **Step 2: 跑局部测试**

Run: `npm test -- src/services/textGuideAnalyzer.test.ts src/store/useRideGuideStore.test.ts`
Expected: PASS

- [ ] **Step 3: 跑总测试**

Run: `npm test -- src/services/textGuideAnalyzer.test.ts src/store/useRideGuideStore.test.ts src/services/kimiClient.test.ts src/utils/promptTemplates.test.ts src/components/SpotGuideCards.test.tsx`
Expected: PASS

- [ ] **Step 4: 跑类型校验**

Run: `npm run check`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/services/textGuideAnalyzer.test.ts src/store/useRideGuideStore.test.ts
git commit -m "test: cover text analysis fallback flow"
```

## 自检

- 规格覆盖：
  - 输入分类：Task 1
  - 关键词型输入：Task 2、Task 4
  - 长文本输入：Task 3、Task 4
  - 链接/口令拦截：Task 1、Task 4
  - store 接入：Task 5
  - UI 提示文案：Task 6
  - Kimi 失败兜底：Task 4、Task 7
- 无 `TODO`、`TBD`、`类似 Task N` 这类占位
- 类型名、函数名和文件路径在各任务中保持一致

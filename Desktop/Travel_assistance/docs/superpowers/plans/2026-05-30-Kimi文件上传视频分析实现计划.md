# Kimi 文件上传视频分析 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将第一阶段视频识别从前端本地抽帧迁移为后端通过 `/v1/files` 上传视频并用 `file_id` 调用 Kimi 完整视频理解。

**Architecture:** 新增一个轻量后端接口处理视频上传、Kimi `/v1/files` 和 `chat/completions` 视频分析，前端只负责提交视频文件并接收 `Stage1SpotExtraction`。第二阶段联网补全与展示链路暂时尽量保留，以降低迁移风险。

**Tech Stack:** TypeScript, React 18, Zustand, Vitest, Vite, Node HTTP handler, Kimi OpenAI-compatible API

---

## File Map

- Create: `c:\Users\夏海鑫\Desktop\旅行项目\api\video-analyze.ts`
  - 后端视频分析入口，接收表单上传并返回 `Stage1SpotExtraction`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\server\kimiFilesClient.ts`
  - 负责上传文件到 `/v1/files`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\server\kimiVideoAnalyzeClient.ts`
  - 负责用 `file_id` 调用 `chat/completions`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\backendVideoAnalyzeClient.ts`
  - 前端调用本地后端接口
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.ts`
  - 第一阶段从本地抽帧改为调用后端视频分析接口
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.test.ts`
  - mock 新的后端视频分析客户端
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\vite-env.d.ts`
  - 补充后端接口地址环境变量类型
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\package.json`
  - 如果需要，补充本地后端开发命令或脚本

### Task 1: 后端 Kimi files 上传与视频分析 client

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\server\kimiFilesClient.ts`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\server\kimiVideoAnalyzeClient.ts`
- Test: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\kimiClient.test.ts`

- [ ] **Step 1: 写失败测试，锁定 file upload 与 file_id 分析链路**

在 `src/services/kimiClient.test.ts` 追加：

```ts
import {
  buildKimiRequestErrorMessage,
} from "@/services/kimiClient"
import {
  analyzeUploadedVideoWithKimi,
} from "@/server/kimiVideoAnalyzeClient"

test("analyzes video by file id instead of inline base64", async () => {
  const fetchMock = vi
    .fn()
    .mockResolvedValueOnce({
      ok: true,
      json: vi.fn().mockResolvedValue({ id: "file_123" }),
    })
    .mockResolvedValueOnce({
      ok: true,
      json: vi.fn().mockResolvedValue({
        choices: [
          {
            message: {
              content: JSON.stringify({
                coreSpotName: "雁荡山",
                city: "温州",
                summary: "山景壮阔",
                highlights: ["灵峰夜景"],
                checkpoints: ["灵岩"],
                foods: ["麦饼"],
                souvenirs: ["文创"],
                nearbyCandidates: ["方洞"],
                transportHints: ["建议自驾"],
                stayHints: ["可住景区附近"],
                tips: ["雨天防滑"],
                confidence: "high",
              }),
            },
          },
        ],
      }),
    })

  vi.stubGlobal("fetch", fetchMock)

  const file = new File([new Uint8Array([1, 2, 3])], "demo.mp4", { type: "video/mp4" })
  const result = await analyzeUploadedVideoWithKimi(file, "kimi-key")

  expect(result.coreSpotName).toBe("雁荡山")
  expect(fetchMock.mock.calls[1]?.[1]?.body).toContain("file_123")
  expect(fetchMock.mock.calls[1]?.[1]?.body).not.toContain("data:video/")
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm run test -- src/services/kimiClient.test.ts`
Expected: FAIL，提示 `@/server/kimiVideoAnalyzeClient` 不存在。

- [ ] **Step 3: 写最小实现**

`src/server/kimiFilesClient.ts`

```ts
const FILES_API_BASE = "https://api.moonshot.cn/v1/files"

export async function uploadVideoToKimi(file: File, apiKey: string): Promise<string> {
  const formData = new FormData()
  formData.append("file", file)
  formData.append("purpose", "file-extract")

  const response = await fetch(FILES_API_BASE, {
    method: "POST",
    headers: {
      Authorization: `Bearer ${apiKey}`,
    },
    body: formData,
  })

  if (!response.ok) {
    const payload = await response.json().catch(() => undefined)
    throw new Error(buildKimiRequestErrorMessage(response.status, payload))
  }

  const payload = (await response.json()) as { id?: string }
  if (!payload.id) throw new Error("KIMI_FILE_UPLOAD_FAILED")
  return payload.id
}
```

`src/server/kimiVideoAnalyzeClient.ts`

```ts
import { uploadVideoToKimi } from "@/server/kimiFilesClient"
import type { Stage1SpotExtraction } from "@/types/spotGuide"

const CHAT_API_BASE = "https://api.moonshot.cn/v1/chat/completions"

export async function analyzeUploadedVideoWithKimi(
  file: File,
  apiKey: string,
): Promise<Stage1SpotExtraction> {
  const fileId = await uploadVideoToKimi(file, apiKey)

  const response = await fetch(CHAT_API_BASE, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${apiKey}`,
    },
    body: JSON.stringify({
      model: "kimi-k2.6",
      temperature: 1,
      response_format: { type: "json_object" },
      messages: [
        {
          role: "system",
          content: "你是景点视频解析助手。请基于完整视频提取景区名称、亮点、打卡点和周边线索，并返回严格 JSON。",
        },
        {
          role: "user",
          content: [
            { type: "text", text: "请分析这个视频并返回景区结构化信息。" },
            { type: "video_url", video_url: { file_id: fileId } },
          ],
        },
      ],
    }),
  })

  if (!response.ok) {
    const payload = await response.json().catch(() => undefined)
    throw new Error(buildKimiRequestErrorMessage(response.status, payload))
  }

  const payload = await response.json() as {
    choices?: Array<{ message?: { content?: string } }>
  }
  return JSON.parse(payload.choices?.[0]?.message?.content ?? "{}") as Stage1SpotExtraction
}
```

- [ ] **Step 4: 重新运行测试确认通过**

Run: `npm run test -- src/services/kimiClient.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add src/server/kimiFilesClient.ts src/server/kimiVideoAnalyzeClient.ts src/services/kimiClient.test.ts
git commit -m "feat: add kimi files upload video analysis client"
```

### Task 2: 新增后端视频分析接口

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\api\video-analyze.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\vite-env.d.ts`

- [ ] **Step 1: 写失败测试，锁定后端接口行为**

在 `src/services/kimiClient.test.ts` 追加：

```ts
import handler from "../../api/video-analyze"

test("returns stage1 extraction from the video analyze api handler", async () => {
  const request = new Request("http://localhost/api/video-analyze", {
    method: "POST",
    body: (() => {
      const formData = new FormData()
      formData.append("video", new File([new Uint8Array([1])], "demo.mp4", { type: "video/mp4" }))
      return formData
    })(),
  })

  const response = await handler(request)
  expect(response.status).not.toBe(404)
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm run test -- src/services/kimiClient.test.ts`
Expected: FAIL，`api/video-analyze.ts` 不存在。

- [ ] **Step 3: 写最小实现**

`api/video-analyze.ts`

```ts
import { analyzeUploadedVideoWithKimi } from "@/server/kimiVideoAnalyzeClient"

export default async function handler(request: Request): Promise<Response> {
  if (request.method !== "POST") {
    return new Response("Method Not Allowed", { status: 405 })
  }

  const formData = await request.formData()
  const file = formData.get("video")

  if (!(file instanceof File)) {
    return Response.json({ error: "MISSING_VIDEO_FILE" }, { status: 400 })
  }

  const apiKey = process.env.KIMI_API_KEY
  if (!apiKey) {
    return Response.json({ error: "MISSING_KIMI_API_KEY" }, { status: 500 })
  }

  try {
    const result = await analyzeUploadedVideoWithKimi(file, apiKey)
    return Response.json(result)
  } catch (error) {
    return Response.json(
      { error: error instanceof Error ? error.message : "VIDEO_ANALYZE_FAILED" },
      { status: 500 },
    )
  }
}
```

`src/vite-env.d.ts`

```ts
interface ImportMetaEnv {
  readonly VITE_KIMI_API_KEY?: string
  readonly VITE_VIDEO_ANALYZE_API_URL?: string
}
```

- [ ] **Step 4: 重新运行测试确认通过**

Run: `npm run test -- src/services/kimiClient.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add api/video-analyze.ts src/vite-env.d.ts src/services/kimiClient.test.ts
git commit -m "feat: add backend video analyze endpoint"
```

### Task 3: 前端改为调用后端视频分析接口

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\backendVideoAnalyzeClient.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.test.ts`

- [ ] **Step 1: 写失败测试，锁定 store 不再依赖本地抽帧主流程**

在 `src/store/useRideGuideStore.test.ts` 追加：

```ts
const { analyzeVideoViaBackendMock } = vi.hoisted(() => ({
  analyzeVideoViaBackendMock: vi.fn(),
}))

vi.mock("@/services/backendVideoAnalyzeClient", () => ({
  analyzeVideoViaBackend: analyzeVideoViaBackendMock,
}))

it("uses backend video analyze api as the primary stage1 path", async () => {
  analyzeVideoViaBackendMock.mockResolvedValue(stage1Fixture)
  enrichSpotWithKimiMock.mockResolvedValue(spotGuideFixture)

  const file = new File([new Uint8Array([1])], "demo.mp4", { type: "video/mp4" })
  await useRideGuideStore.getState().analyzeDemoVideo(file)

  expect(analyzeVideoViaBackendMock).toHaveBeenCalledTimes(1)
  expect(analyzeVideoWithKimiMock).not.toHaveBeenCalled()
  expect(useRideGuideStore.getState().status).toBe("success")
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm run test -- src/store/useRideGuideStore.test.ts`
Expected: FAIL，当前 store 仍走本地抽帧和 `analyzeVideoWithKimi`。

- [ ] **Step 3: 写最小实现**

`src/services/backendVideoAnalyzeClient.ts`

```ts
import type { Stage1SpotExtraction } from "@/types/spotGuide"

export async function analyzeVideoViaBackend(file: File): Promise<Stage1SpotExtraction> {
  const url = import.meta.env.VITE_VIDEO_ANALYZE_API_URL || "/api/video-analyze"
  const formData = new FormData()
  formData.append("video", file)

  const response = await fetch(url, {
    method: "POST",
    body: formData,
  })

  const payload = await response.json()
  if (!response.ok) {
    throw new Error(payload?.error || "VIDEO_ANALYZE_FAILED")
  }

  return payload as Stage1SpotExtraction
}
```

将 `src/store/useRideGuideStore.ts` 中第一阶段替换为：

```ts
set({ status: "reading_video", videoAnalyzeWaitLevel: "normal" })

const stage1 = await analyzeVideoViaBackend(file)
```

保留现有第二阶段、候选景区推断和展示逻辑不变。

- [ ] **Step 4: 重新运行测试确认通过**

Run: `npm run test -- src/store/useRideGuideStore.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add src/services/backendVideoAnalyzeClient.ts src/store/useRideGuideStore.ts src/store/useRideGuideStore.test.ts
git commit -m "feat: move stage1 video analysis to backend"
```

### Task 4: 回归验证与收尾

**Files:**
- Verify: `c:\Users\夏海鑫\Desktop\旅行项目\src\server\kimiFilesClient.ts`
- Verify: `c:\Users\夏海鑫\Desktop\旅行项目\src\server\kimiVideoAnalyzeClient.ts`
- Verify: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\backendVideoAnalyzeClient.ts`
- Verify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.ts`

- [ ] **Step 1: 跑相关测试**

Run: `npm run test -- src/services/kimiClient.test.ts src/store/useRideGuideStore.test.ts`
Expected: PASS

- [ ] **Step 2: 跑类型检查与构建**

Run: `npm run check`
Expected: PASS

Run: `npm run build`
Expected: PASS

- [ ] **Step 3: 提交最终收尾**

```bash
git add api/video-analyze.ts src/server/kimiFilesClient.ts src/server/kimiVideoAnalyzeClient.ts src/services/backendVideoAnalyzeClient.ts src/store/useRideGuideStore.ts src/store/useRideGuideStore.test.ts src/vite-env.d.ts src/services/kimiClient.test.ts
git commit -m "feat: analyze videos via kimi files upload pipeline"
```

## Self-Review

- Spec coverage:
  - 后端 `/v1/files` 上传由 Task 1 覆盖
  - 后端视频分析接口由 Task 2 覆盖
  - 前端迁移第一阶段到后端由 Task 3 覆盖
  - 测试与构建验证由 Task 4 覆盖
- Placeholder scan:
  - 无 `TODO`、`TBD`、`later`
  - 每个任务均包含文件、代码、命令和预期结果
- Type consistency:
  - 后端入口统一为 `analyzeUploadedVideoWithKimi`
  - 前端服务统一为 `analyzeVideoViaBackend`
  - 后端接口统一为 `/api/video-analyze`

Plan complete and saved to `docs/superpowers/plans/2026-05-30-Kimi文件上传视频分析实现计划.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?

# 景点兜底补全 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让视频分析完成后始终尽量展示景区名称和部分景点内容，并在识别不足时自动触发线索联网补全与候选景区推断。

**Architecture:** 在现有抽帧识别与联网补全链路上新增“结果充分性判断”与“两级补救”。第一层使用现有视频线索直接联网补全；第二层在景区名仍缺失时推断候选景区并再次联网补全。所有第二阶段结果在进入 UI 前统一归一化，并增加 `full / partial / inferred` 三种展示模式。

**Tech Stack:** React 18, TypeScript, Zustand, Vitest, Vite, Kimi OpenAI-compatible API

---

## File Map

- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\spotGuideCompleteness.ts`
  - 判断当前结果是否足够展示
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\prompts\kimi-spot-inference.template.json`
  - 候选景区推断模板
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\promptTemplates.ts`
  - 新增候选景区推断请求构造
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\promptTemplates.test.ts`
  - 锁定推断请求格式
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\types\spotGuide.ts`
  - 为展示模式和提示文案增加字段
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\kimiClient.ts`
  - 新增候选景区推断调用、线索补全调用、结果归一化增强
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\kimiClient.test.ts`
  - 覆盖候选景区推断与归一化
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.ts`
  - 新增结果充分性判断、线索补全、候选景区推断与二次补全
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.test.ts`
  - 覆盖三段式结果生成链路
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\components\SpotGuideCards.tsx`
  - 展示 `inferred` 模式提示和兜底文案
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\components\SpotGuideCards.test.tsx`
  - 覆盖候选景区结果展示
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\components\ResultPanel.tsx`
  - 传递展示模式

### Task 1: 结果充分性判断与推断模板

**Files:**
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\spotGuideCompleteness.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\promptTemplates.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\promptTemplates.test.ts`
- Create: `c:\Users\夏海鑫\Desktop\旅行项目\src\prompts\kimi-spot-inference.template.json`

- [ ] **Step 1: 写失败测试，锁定结果充分性和推断请求格式**

```ts
import { describe, expect, it } from "vitest"
import {
  buildSpotInferenceRequest,
  buildSpotEnrichmentRequest,
} from "@/utils/promptTemplates"
import { isSpotGuideContentSufficient } from "@/utils/spotGuideCompleteness"

describe("isSpotGuideContentSufficient", () => {
  it("returns false when there is no title and no content sections", () => {
    expect(isSpotGuideContentSufficient({
      coreSpot: {
        title: "",
        city: "温州",
        summary: "",
        audienceTags: [],
      },
      highlights: [],
      checkpoints: [],
      foodAndSouvenirs: [],
      nearbyRecommendations: [],
      extraInfo: {
        transportTags: [],
        stayTags: [],
        tips: [],
      },
      source: {
        rawInput: "demo",
        kind: "text",
      },
    })).toBe(false)
  })

  it("returns true when title exists and at least one section has content", () => {
    expect(isSpotGuideContentSufficient({
      coreSpot: {
        title: "雁荡山",
        city: "温州",
        summary: "山景壮阔",
        audienceTags: [],
      },
      highlights: [{ title: "灵峰夜景", description: "夜景知名" }],
      checkpoints: [],
      foodAndSouvenirs: [],
      nearbyRecommendations: [],
      extraInfo: {
        transportTags: [],
        stayTags: [],
        tips: [],
      },
      source: {
        rawInput: "demo",
        kind: "text",
      },
    })).toBe(true)
  })
})

describe("buildSpotInferenceRequest", () => {
  it("builds an inference request from weak stage1 clues", async () => {
    const request = await buildSpotInferenceRequest({
      coreSpotName: "",
      city: "温州",
      summary: "",
      highlights: ["山峰", "夜景"],
      checkpoints: ["观景台"],
      foods: [],
      souvenirs: [],
      nearbyCandidates: ["景区索道"],
      transportHints: [],
      stayHints: [],
      tips: [],
      confidence: "low",
    })

    expect(request.messages[1]?.content).toContain("\"highlights\":[\"山峰\",\"夜景\"]")
    expect(request.response_format).toEqual({ type: "json_object" })
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm run test -- src/utils/promptTemplates.test.ts`
Expected: FAIL，提示 `spotGuideCompleteness` 和 `buildSpotInferenceRequest` 尚不存在。

- [ ] **Step 3: 写最小实现**

`src/utils/spotGuideCompleteness.ts`

```ts
import type { SpotGuide } from "@/types/spotGuide"

export function isSpotGuideContentSufficient(guide: SpotGuide): boolean {
  const hasTitle = Boolean(guide.coreSpot.title?.trim())
  const hasSummary = Boolean(guide.coreSpot.summary?.trim())
  const hasSections =
    guide.highlights.length > 0 ||
    guide.checkpoints.length > 0 ||
    guide.foodAndSouvenirs.length > 0 ||
    guide.nearbyRecommendations.length > 0

  return hasTitle && (hasSummary || hasSections)
}
```

`src/prompts/kimi-spot-inference.template.json`

```json
{
  "model": "kimi-k2.6",
  "temperature": 1,
  "response_format": { "type": "json_object" },
  "system_prompt": "你是景区候选推断助手。请基于给定的视频提取线索，推断最可能的景区名称，并返回严格 JSON，不要输出额外解释。",
  "user_prompt_template": "请根据以下视频线索推断最可能的景区名称，并返回 JSON：{{STAGE1_JSON}}"
}
```

`src/utils/promptTemplates.ts`

```ts
import spotInferenceTemplate from "@/prompts/kimi-spot-inference.template.json"

export async function buildSpotInferenceRequest(stage1: Stage1SpotExtraction) {
  return {
    model: spotInferenceTemplate.model,
    temperature: spotInferenceTemplate.temperature,
    response_format: spotInferenceTemplate.response_format,
    messages: [
      {
        role: "system",
        content: spotInferenceTemplate.system_prompt,
      } satisfies ChatMessage,
      {
        role: "user",
        content: replacePlaceholder(
          spotInferenceTemplate.user_prompt_template,
          "{{STAGE1_JSON}}",
          JSON.stringify(stage1),
        ),
      } satisfies ChatMessage,
    ],
  }
}
```

- [ ] **Step 4: 重新运行测试确认通过**

Run: `npm run test -- src/utils/promptTemplates.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add src/utils/spotGuideCompleteness.ts src/prompts/kimi-spot-inference.template.json src/utils/promptTemplates.ts src/utils/promptTemplates.test.ts
git commit -m "test: lock spot guide fallback inference inputs"
```

### Task 2: Kimi client 增加候选景区推断与补全归一化

**Files:**
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\types\spotGuide.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\kimiClient.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\services\kimiClient.test.ts`

- [ ] **Step 1: 写失败测试，锁定候选景区推断与展示模式**

在 `src/services/kimiClient.test.ts` 追加：

```ts
test("infers a candidate spot name from weak stage1 clues", async () => {
  vi.stubGlobal("window", { __KIMI_API_KEY__: "window-key" })
  vi.stubGlobal("fetch", vi.fn().mockResolvedValue({
    ok: true,
    json: vi.fn().mockResolvedValue({
      choices: [
        {
          message: {
            content: JSON.stringify({
              inferredSpotName: "雁荡山",
              rationale: "画面出现山峰、夜景和观景台线索",
            }),
          },
        },
      ],
    }),
  }))

  const result = await inferSpotCandidateWithKimi({
    coreSpotName: "",
    city: "温州",
    summary: "",
    highlights: ["山峰", "夜景"],
    checkpoints: ["观景台"],
    foods: [],
    souvenirs: [],
    nearbyCandidates: ["索道"],
    transportHints: [],
    stayHints: [],
    tips: [],
    confidence: "low",
  })

  expect(result).toEqual({
    inferredSpotName: "雁荡山",
    rationale: "画面出现山峰、夜景和观景台线索",
  })
})

test("normalizes inferred guide metadata for display", async () => {
  vi.stubGlobal("window", { __KIMI_API_KEY__: "window-key" })
  vi.stubGlobal("fetch", vi.fn().mockResolvedValue({
    ok: true,
    json: vi.fn().mockResolvedValue({
      choices: [
        {
          message: {
            content: JSON.stringify({
              coreSpot: {
                title: "雁荡山",
                city: "温州",
                summary: "山景壮阔",
                audienceTags: ["山岳"],
              },
              highlights: [],
              checkpoints: [],
              foodAndSouvenirs: [],
              nearbyRecommendations: [],
              extraInfo: {
                transportTags: [],
                stayTags: [],
                tips: [],
              },
            }),
          },
        },
      ],
    }),
  }))

  const result = await enrichSpotWithKimi(
    {
      coreSpotName: "",
      city: "温州",
      summary: "",
      highlights: ["山峰"],
      checkpoints: [],
      foods: [],
      souvenirs: [],
      nearbyCandidates: [],
      transportHints: [],
      stayHints: [],
      tips: [],
      confidence: "low",
    },
    {
      displayMode: "inferred",
      displayHints: ["该景区名称根据视频线索推断，建议进一步核验。"],
    },
  )

  expect(result.displayMode).toBe("inferred")
  expect(result.displayHints).toContain("该景区名称根据视频线索推断，建议进一步核验。")
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm run test -- src/services/kimiClient.test.ts`
Expected: FAIL，提示 `inferSpotCandidateWithKimi` 不存在，`SpotGuide` 不支持 `displayMode`。

- [ ] **Step 3: 写最小实现**

`src/types/spotGuide.ts`

```ts
export type SpotGuideDisplayMode = "full" | "partial" | "inferred"

export type SpotGuide = {
  source: {
    rawInput: string
    kind: "url" | "text"
  }
  coreSpot: {
    title: string
    city?: string
    summary: string
    bestTime?: string
    audienceTags: string[]
  }
  highlights: Array<{ title: string; description: string }>
  checkpoints: Array<{ name: string; description: string; photoTip?: string; stayHint?: string }>
  foodAndSouvenirs: Array<{ name: string; category: "food" | "souvenir"; reason: string }>
  nearbyRecommendations: Array<{ name: string; reason: string; relationHint?: string }>
  extraInfo: {
    transportTags: string[]
    stayTags: string[]
    durationHint?: string
    tips: string[]
  }
  displayMode?: SpotGuideDisplayMode
  displayHints?: string[]
}
```

`src/services/kimiClient.ts`

```ts
type SpotCandidate = {
  inferredSpotName: string
  rationale: string
}

export async function inferSpotCandidateWithKimi(
  stage1: Stage1SpotExtraction,
): Promise<SpotCandidate> {
  const body = await buildSpotInferenceRequest(stage1)
  const response = await postJson(body)
  return parseMessageContentAsJson<SpotCandidate>(response)
}

function applyDisplayMetadata(
  guide: SpotGuide,
  options?: Pick<SpotGuide, "displayMode" | "displayHints">,
): SpotGuide {
  return {
    ...guide,
    displayMode: options?.displayMode ?? guide.displayMode ?? "full",
    displayHints: options?.displayHints ?? guide.displayHints ?? [],
  }
}
```

并将 `enrichSpotWithKimi()` 的签名改为：

```ts
export async function enrichSpotWithKimi(
  stage1: Stage1SpotExtraction,
  options?: Pick<SpotGuide, "displayMode" | "displayHints">,
): Promise<SpotGuide> {
  const body = await buildSpotEnrichmentRequest(stage1)
  const response = await completeBuiltinToolCalls(body)
  return applyDisplayMetadata(
    normalizeSpotGuide(parseMessageContentAsJson<SpotGuideLike>(response)),
    options,
  )
}
```

- [ ] **Step 4: 重新运行测试确认通过**

Run: `npm run test -- src/services/kimiClient.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add src/types/spotGuide.ts src/services/kimiClient.ts src/services/kimiClient.test.ts
git commit -m "feat: add inferred spot fallback metadata"
```

### Task 3: Store 接入线索直补与候选景区推断

**Files:**
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\store\useRideGuideStore.test.ts`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\utils\spotGuideMapper.ts`

- [ ] **Step 1: 写失败测试，锁定三段式兜底链路**

在 `src/store/useRideGuideStore.test.ts` 追加：

```ts
const { inferSpotCandidateWithKimiMock } = vi.hoisted(() => ({
  inferSpotCandidateWithKimiMock: vi.fn(),
}))

vi.mock("@/services/kimiClient", async () => {
  const actual = await vi.importActual<typeof import("@/services/kimiClient")>("@/services/kimiClient")
  return {
    ...actual,
    analyzeVideoWithKimi: analyzeVideoWithKimiMock,
    enrichSpotWithKimi: enrichSpotWithKimiMock,
    inferSpotCandidateWithKimi: inferSpotCandidateWithKimiMock,
  }
})

it("tries direct enrichment first and then inferred spot enrichment when extracted content is insufficient", async () => {
  analyzeVideoWithKimiMock.mockResolvedValue({
    coreSpotName: "",
    city: "温州",
    summary: "",
    highlights: [],
    checkpoints: [],
    foods: [],
    souvenirs: [],
    nearbyCandidates: ["索道"],
    transportHints: [],
    stayHints: [],
    tips: [],
    confidence: "low",
  })

  enrichSpotWithKimiMock
    .mockResolvedValueOnce({
      ...spotGuideFixture,
      coreSpot: {
        ...spotGuideFixture.coreSpot,
        title: "",
        summary: "",
      },
      highlights: [],
      checkpoints: [],
      foodAndSouvenirs: [],
      nearbyRecommendations: [],
    })
    .mockResolvedValueOnce({
      ...spotGuideFixture,
      displayMode: "inferred",
      displayHints: ["该景区名称根据视频线索推断，建议进一步核验。"],
    })

  inferSpotCandidateWithKimiMock.mockResolvedValue({
    inferredSpotName: "雁荡山",
    rationale: "出现山峰与索道线索",
  })

  const file = new File([new Uint8Array([1])], "demo.mp4", { type: "video/mp4" })
  await useRideGuideStore.getState().analyzeDemoVideo(file)

  expect(enrichSpotWithKimiMock).toHaveBeenCalledTimes(2)
  expect(inferSpotCandidateWithKimiMock).toHaveBeenCalledTimes(1)
  expect(useRideGuideStore.getState().result?.coreSpot.title).toBe("雁荡山")
  expect(useRideGuideStore.getState().result?.displayMode).toBe("inferred")
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm run test -- src/store/useRideGuideStore.test.ts`
Expected: FAIL，当前 store 只执行一次补全，没有结果充分性判断。

- [ ] **Step 3: 写最小实现**

在 `src/store/useRideGuideStore.ts` 增加新流程：

```ts
const directGuide = await enrichSpotWithKimi(stage1)

if (isSpotGuideContentSufficient(directGuide)) {
  set({
    status: "success",
    result: directGuide,
    partialMessage: undefined,
    videoAnalyzeWaitLevel: "normal",
  })
  return
}

const inferred = await inferSpotCandidateWithKimi(stage1)
const inferredStage1 = {
  ...stage1,
  coreSpotName: inferred.inferredSpotName,
}

const inferredGuide = await enrichSpotWithKimi(inferredStage1, {
  displayMode: "inferred",
  displayHints: ["该景区名称根据视频线索推断，建议进一步核验。"],
})

set({
  status: "success",
  result: inferredGuide,
  partialMessage: undefined,
  videoAnalyzeWaitLevel: "normal",
})
```

并保留最后的 `partial_success` 兜底：

```ts
set({
  status: "partial_success",
  result: mapStage1ToSpotGuide(stage1),
  partialMessage: `联网补充失败${detail}。当前展示的是视频提取结果`,
  videoAnalyzeWaitLevel: "normal",
})
```

- [ ] **Step 4: 重新运行测试确认通过**

Run: `npm run test -- src/store/useRideGuideStore.test.ts`
Expected: PASS

- [ ] **Step 5: 提交这一小步**

```bash
git add src/store/useRideGuideStore.ts src/store/useRideGuideStore.test.ts src/utils/spotGuideMapper.ts
git commit -m "feat: fallback to inferred scenic spot enrichment"
```

### Task 4: 页面展示推断结果与收尾验证

**Files:**
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\components\SpotGuideCards.tsx`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\components\SpotGuideCards.test.tsx`
- Modify: `c:\Users\夏海鑫\Desktop\旅行项目\src\components\ResultPanel.tsx`

- [ ] **Step 1: 写失败测试，锁定 inferred 模式提示**

在 `src/components/SpotGuideCards.test.tsx` 追加：

```tsx
it("shows inference guidance when the scenic spot name is inferred", () => {
  const markup = renderToStaticMarkup(
    <SpotGuideCards
      mode="inferred"
      guide={{
        source: { rawInput: "demo", kind: "text" },
        coreSpot: {
          title: "雁荡山",
          city: "温州",
          summary: "山景壮阔",
          audienceTags: ["山岳"],
        },
        highlights: [],
        checkpoints: [],
        foodAndSouvenirs: [],
        nearbyRecommendations: [],
        extraInfo: {
          transportTags: [],
          stayTags: [],
          tips: [],
        },
        displayMode: "inferred",
        displayHints: ["该景区名称根据视频线索推断，建议进一步核验。"],
      }}
    />,
  )

  expect(markup).toContain("该景区名称根据视频线索推断，建议进一步核验。")
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm run test -- src/components/SpotGuideCards.test.tsx`
Expected: FAIL，当前组件没有 `inferred` 模式提示。

- [ ] **Step 3: 写最小实现**

在 `src/components/SpotGuideCards.tsx` 中扩展模式：

```tsx
const isPartial = mode === "partial"
const isInferred = mode === "inferred"

{isPartial ? (
  <SectionCard className="border-brand-orange/20 bg-brand-orange/10 shadow-none">
    <div className="text-sm text-ink-700">
      当前展示的是视频提取版结果，部分栏目可能为空；联网补充成功后会展示更完整的信息。
    </div>
  </SectionCard>
) : null}

{isInferred ? (
  <SectionCard className="border-brand-teal/20 bg-brand-teal/10 shadow-none">
    <div className="text-sm text-ink-700">
      {guide.displayHints?.[0] ?? "该景区名称根据视频线索推断，建议进一步核验。"}
    </div>
  </SectionCard>
) : null}
```

在 `src/components/ResultPanel.tsx` 中传递模式：

```tsx
return (
  <SpotGuideCards
    guide={guide}
    mode={guide.displayMode ?? (status === "partial_success" ? "partial" : "full")}
  />
)
```

- [ ] **Step 4: 重新运行前端测试**

Run: `npm run test -- src/components/SpotGuideCards.test.tsx`
Expected: PASS

- [ ] **Step 5: 跑总回归**

Run: `npm run test -- src/utils/promptTemplates.test.ts src/services/kimiClient.test.ts src/store/useRideGuideStore.test.ts src/utils/spotGuideMapper.test.ts src/components/SpotGuideCards.test.tsx`
Expected: PASS

Run: `npm run check`
Expected: PASS

Run: `npm run build`
Expected: PASS

- [ ] **Step 6: 提交最终收尾**

```bash
git add src/utils/spotGuideCompleteness.ts src/prompts/kimi-spot-inference.template.json src/utils/promptTemplates.ts src/utils/promptTemplates.test.ts src/types/spotGuide.ts src/services/kimiClient.ts src/services/kimiClient.test.ts src/store/useRideGuideStore.ts src/store/useRideGuideStore.test.ts src/components/SpotGuideCards.tsx src/components/SpotGuideCards.test.tsx src/components/ResultPanel.tsx
git commit -m "feat: guarantee scenic spot fallback content after video analysis"
```

## Self-Review

- Spec coverage:
  - 结果充分性判断由 Task 1 覆盖
  - 线索直补与候选景区推断由 Task 2 和 Task 3 覆盖
  - `full / partial / inferred` 展示模式由 Task 2 和 Task 4 覆盖
  - 结果归一化与防白屏要求由 Task 2 覆盖
- Placeholder scan:
  - 无 `TBD`、`TODO`、`later`
  - 每个任务均包含代码、命令和预期
- Type consistency:
  - `displayMode` 统一使用 `full | partial | inferred`
  - 推断接口统一为 `inferSpotCandidateWithKimi`
  - 结果充分性统一使用 `isSpotGuideContentSufficient`

Plan complete and saved to `docs/superpowers/plans/2026-05-30-景点兜底补全实现计划.md`. Two execution options:

**1. Subagent-Driven (recommended)** - I dispatch a fresh subagent per task, review between tasks, fast iteration

**2. Inline Execution** - Execute tasks in this session using executing-plans, batch execution with checkpoints

Which approach?
